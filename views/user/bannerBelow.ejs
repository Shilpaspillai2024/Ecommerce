<style>
    .banner-bottom-container {
        display: flex;
        flex-direction: row;
        justify-content: space-around;
        background: linear-gradient(120deg, rgb(155, 197, 58), #03030385);
        border-radius: 10px;
        padding: 2rem 0;
        margin-bottom: 5rem;
        margin-top: 3rem;
        transition: transform 0.3s ease-in-out;
    }

    .banner-bottom-container:hover{
        transform: scale(1.1);
    }

    .banner-inner-box {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        transition: transform 0.3s ease-in-out; /* Smooth transition for scaling */
    }

    .banner-content-box {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background-color: #000000;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1rem;
        transition: transform 0.3s ease-in-out; /* Smooth transition for scaling */
    }

    .banner-content-box i {
        color: #ffffff;
        scale: 1.5;
        transition: transform 0.3s ease-in-out; /* Smooth transition for scaling */
    }

    .banner-inner-box h5,
    p {
        color: white;
    }
    .banner-inner-box:hover .banner-content-box {
    transform: scale(1.1); /* Scale up the icon box on hover */
}

.banner-inner-box:hover {
    transform: scale(1.05); /* Scale up the inner box on hover */
}

/* Responsive styles */
@media (max-width: 768px) {
    .banner-bottom-container {
        flex-direction: column;
        align-items: center;
    }

    .banner-inner-box {
        margin-bottom: 2rem;
    }
}
</style>


<div class="container banner-bottom-container">
    <div class="banner-inner-box">
        <div class="banner-content-box">
            <i class="fa-solid fa-truck-fast"></i>
        </div>
        <h5>Free and Fast Delivery</h5>
        <p> Free delivery for above order 50 rupees</p>

    </div>
    <div class="banner-inner-box">
        <div class="banner-content-box">
            <i class="fa-solid fa-headset"></i>
        </div>
        <h5>24/7 Customer Service</h5>
        <p>Friendly customer support</p>
    </div>

</div>